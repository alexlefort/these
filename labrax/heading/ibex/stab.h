#include "tstab.h" 
 
 using namespace std; 
 using namespace ibex; 
 
Function stab(x,p,ibex::max(ibex::max(ibex::max(ibex::max(-stab5,-stab3),-stab1),ibex::pow(stab2,2)*ibex::pow(stab5,2) + stab1*ibex::pow(stab4,2)*stab5 - stab2*stab3*stab4*stab5),stab1*stab4 - stab2*stab3)); 
